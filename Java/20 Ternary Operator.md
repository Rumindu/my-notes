# Ternary Operator
``` java 
public class Main {
    public static void main(String[] args) {
        int income = 120_000;
        //If the condition is true, the first value is assigned to the variable.
        // Otherwise, the second value is assigned.
        String className = income > 100_000 ? "First" : "Economy";
    }
}
```